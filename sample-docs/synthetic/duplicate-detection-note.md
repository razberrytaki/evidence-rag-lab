---
id: duplicate-detection-note
sourceType: synthetic-conflict
licenseNote: Synthetic fixture.
---

# Duplicate Detection Note

Duplicate detection prevents repeated chunks from crowding out distinct
evidence. The first pass uses content hashes and document version identifiers so
the retrieval trace can explain whether a candidate was unique, repeated, or
superseded by a newer source.
